# P3107R5 and P3235R3 print implementation

## Scope

P3107R5 permits `std::print` to avoid formatting the complete result into a
`std::string` when every argument opts into
`enable_nonlocking_formatter_optimization`. P3235R3 extends that opt-in to
additional standard-library formatters. Neither paper specifies a particular
buffer class, nor does P3235R3 require a second parser for bare `{}` fields.
The live C++ working draft retains `vformat` for the deliberately buffered
`vprint_*_buffered` functions and specifies the non-buffered functions in
terms of output while holding the stream lock.

## Design

The non-buffered FILE* and `ostream` implementations will call `vformat_to`
with a libc++ internal output iterator. The iterator owns a fixed-size byte
or wide-character buffer and flushes bounded chunks to its target. It never
owns the complete formatted result. A final explicit flush is required;
destruction does not perform I/O, so write errors are reported before the
print function returns.

The FILE* adapter uses `fwrite` and preserves the existing `feof`/`ferror`
mapping to `system_error(EIO)` or the stream's error number. The `ostream`
adapter uses `sputn`; a short write sets `badbit | failbit` through the
existing exception-aware path. Formatting exceptions remain outside that
write-error conversion path, preserving the sentry and exception-mask rules.

The Windows native Unicode-terminal path remains string-based in the first
implementation step. It must transcode UTF-8 to UTF-16 before calling
`WriteConsoleW`, and its existing intermediate buffer has different element
type and ownership requirements. The non-terminal path still uses the direct
FILE* writer.

The existing `vprint_*_buffered` semantics are unchanged. Tests will inspect
write call sizes for output larger than the internal buffer and inject a
mid-output write failure, in addition to checking byte-for-byte output.

`print`/`println` compute `locksafe` — `(enable_nonlocking_formatter_optimization<Args> && ...)`
per [print.fun] — at each call site that still has the concrete argument
types, dispatching to the non-buffered writer when true and to a
`_buffered` fallback otherwise. The type-erased `vprint_unicode`/
`vprint_nonunicode` overloads (which take an already-erased `format_args`,
with no compile-time type information to compute `locksafe`) always use the
`_buffered` path. The `ostream` `_buffered` fallback for the unicode path
must construct its `sentry` before calling `vformat`, not after — a
tied-stream sync failure must take precedence over a format error per
[ostream.formatted.print]/3.2, and calling `vformat` unconditionally first
inverts that precedence.

**Correction, post-review**: an earlier version of this implementation
speculatively opted several more types into
`enable_nonlocking_formatter_optimization` (the calendar chrono types,
`sys_time`/`utc_time`/`tai_time`/`gps_time`/`file_time`/`local_time`,
`hh_mm_ss`, `thread::id`, `filesystem::path`, `stacktrace_entry`/
`basic_stacktrace`, `tuple`/`pair`) based on P3235R3's prose description of
its own motivation. Checked directly against both the paper's own adopted
wording and the live draft at eel.is/c++draft/time.format: **the adopted
wording only specializes this trait for `chrono::duration` (inherited from
its representation) and `chrono::zoned_time` with the default
`TimeZonePtr`** — nothing else. The speculative opt-ins were reverted; the
pre-existing `enable_nonlocking_formatter_optimization.compile.pass.cpp`
test's assertions for these types (which predate P3235R3 and correctly
assert `false`) were left untouched, since they're still correct. Only the
two assertions for `duration`/`zoned_time` were updated to match the
verified new behavior.

References: [P3107R5](https://wg21.link/P3107R5),
[P3235R3](https://wg21.link/P3235R3), the live
[print wording](https://eel.is/c++draft/print.fun), and
[time.format](https://eel.is/c++draft/time.format).
