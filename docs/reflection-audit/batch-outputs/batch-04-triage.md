| Issue # | Title (short) | Disposition | Evidence/Reasoning (2-4 sentences, cite file:line where looked) | Confidence |
|---:|---|---|---|---|
| 311 | Builtin-template diagnostic ICE | **Confirmed-Open** | `DescriptionOf` handles several ordinary template kinds but ends in `llvm_unreachable("unhandled template kind")` for anything else (`clang/lib/AST/ExprConstantMeta.cpp:1749-1771`). Current reflection enumeration exposes builtin templates, and diagnostic paths call `DescriptionOf` repeatedly. No later history or test covers `BuiltinTemplateDecl`. | High |
| 312 | Deduction-guide specialization mangling ICE | **Confirmed-Open** | Reflection mangling still routes ordinary declaration reflections through `mangle(cast<NamedDecl>(D))` (`clang/lib/AST/ItaniumMangle.cpp:4898-4917`). The mangler still explicitly reaches `llvm_unreachable("Can't mangle a deduction guide name!")` (`clang/lib/AST/ItaniumMangle.cpp:1444`, `1738`). The fork’s later mangling fix only added unnamed-field support, not deduction guides. | High |
| 313 | `members_of` truncates after linkage specifier | **Already-Fixed** | `findIterableMember` now explicitly descends into `LinkageSpecDecl` and pops back out when its local chain ends (`clang/lib/AST/ExprConstantMeta.cpp:1551-1560`). This is directly the failure mode described by the issue. The behavior was introduced by the fork’s namespace/linkage traversal reconciliation, with no matching current regression evidence. | High |
| 314 | LP64 NEON vector mangling ICE | **Confirmed-Open** | `mangleNeonVectorType` still accepts only `LongLong`/`ULongLong` for 64-bit integer elements and otherwise executes `llvm_unreachable("unexpected Neon vector element type")` (`clang/lib/AST/ItaniumMangle.cpp:3941-3959`). LP64 `long` remains uncovered. This is reflection-independent, but still a correctness/robustness bug in this fork. | High |
| 319 | `op_caret_equals` symbol typo | **Confirmed-Open** | Both operator tables still contain `"^"`/`u8"^"` at the `op_caret_equals` slot (`libcxx/include/meta:906-925`), exactly matching the report. No regression test for compound-assignment spellings appears in the reflection libc++ tests. | High |
| 321 | 32K+ template packs miscompile | **Confirmed-Open** | The AST still stores `SubstNonTypeTemplateParmPackExpr::NumArguments` in a 15-bit bitfield (`clang/include/clang/AST/ExprCXX.h:4761-4778`). No fork commit or test widened this field; `define_static_string` remains pack-based (`libcxx/include/meta:2657-2663`). | High |
| 322 | Parameter-name result depends on instantiation | **Already-Fixed** | `getParameterName` now switches to the template-instantiation pattern before walking redeclarations (`clang/lib/AST/ExprConstantMeta.cpp:1130-1148`). Commit `fad02ea72cc7` added this implementation and `param-name-consistency-instantiation.pass.cpp` tests both inconsistent and consistent names. | High |
| 326 | Expansion statement ICE during instantiation | **Confirmed-Open** | `TransformCXXIndeterminateExpansionSelectExpr` still rebuilds through `BuildCXXExpansionSelectExpr` (`clang/lib/Sema/TreeTransform.h:9544-9573`), which calls iterable-range handling without rejecting unresolved overload ranges (`clang/lib/Sema/SemaExpand.cpp:439-460`). The current `tryMakeCXXIterableExpansionSelectExpr` proceeds into ADL/candidate construction on the range (`clang/lib/Sema/SemaExpand.cpp:190-219`). | High |
| 327 | Expansion statement ICE on unresolved overload range | **Confirmed-Open** | The same unresolved range reaches `AddArgumentDependentLookupCandidates` in `tryMakeCXXIterableExpansionSelectExpr` (`clang/lib/Sema/SemaExpand.cpp:190-219`). The fork has expansion-statement tests, but none exercises an unresolved overloaded function or uncalled function template as the range. | High |
| 329 | Constant evaluation crash through PCH | **Confirmed-Open** | `CXXMetafunctionExpr` arguments are serialized as ordinary AST statements (`clang/lib/Serialization/ASTWriterStmt.cpp:498-515`), while the fork’s architecture still relies on evaluator-side reflection/metafunction state. The issue’s PCH-specific failure is therefore plausible and no current PCH/module regression test covers a metafunction calling `members_of`. | Medium |
| 331 | `reflect_object` rejects explicit defaulted copy constructor | **Needs-Build-To-Verify** | `object_of`/the corresponding reflection-value path evaluates synthesized lvalues and relies on ordinary constant evaluation (`clang/lib/AST/ExprConstantMeta.cpp:3170-3207`). Source inspection shows no explicit exception for an explicitly declared defaulted copy constructor, but the exact rejection depends on evaluator classification and cannot be established without compiling. Repro: `struct A { std::meta::info _; A() = default; A(const A&) = default; } a{}; consteval { std::meta::reflect_object(a); }`. | Medium |
| 332 | `reflect_constant` rejects pointer to mixed consteval-only type | **Needs-Build-To-Verify** | `constant_of` validates structural type and then evaluates a synthesized `ConstantExpr` (`clang/lib/AST/ExprConstantMeta.cpp:3227-3264`); the generic consteval-only smuggling check can reject lvalues based on their base expression/type (`clang/lib/AST/ExprConstant.cpp:2405-2410`). Whether the specific combination of a consteval-only member and consteval member function reaches that check requires a build run. Repro: `constexpr struct { std::meta::info _; consteval auto f() const {} } a{}; consteval { std::meta::reflect_constant(&a); }`. | Medium |
| 333 | Splice operand convertible to `meta::info` rejected | **Already-Fixed** | `BuildSpliceSpecifier` first performs `DefaultLvalueConversion`, then explicitly performs an implicit conversion to `Context.MetaInfoTy` when needed (`clang/lib/Sema/SemaReflect.cpp:1575-1593`). This directly supports `[:A{}:]` where `A` has a `consteval operator info()`. | High |
| 334 | Static member call inherits consteval-only object restriction | **Confirmed-Open** | Constant evaluation still rejects an expression when its base expression has a consteval-only type, unless the result type itself is consteval-only (`clang/lib/AST/ExprConstant.cpp:2405-2410`). The check does not distinguish a static member call whose object expression is unevaluated, matching the reported failure. | Medium |
| 342 | `^^derived::operator()` rejects using-declaration | **Confirmed-Open** | `ActOnCXXReflectExpr` unconditionally rejects a representative `UsingShadowDecl` unless entity-proxy reflection is enabled (`clang/lib/Sema/SemaReflect.cpp:1042-1050`). The later `BuildCXXReflectExpr` path retains the same rejection (`clang/lib/Sema/SemaReflect.cpp:1320-1339`), with no distinction for operator-function-id lookup. | High |
| 346 | Spurious warning for reflected reference type | **Needs-Build-To-Verify** | The warning is defined as a generic token-binding warning (`clang/include/clang/Basic/DiagnosticParseKinds.td:1828-1829`) and emitted from `ParseReflect.cpp:149`; source inspection does not show whether the parser has enough type information to suppress it for `decltype(std::move(1))`. Repro: `static_assert(^^decltype(std::move(1)) != ^^int);`. | Medium |
| 350 | `->[:member:]` assertion with lvalue pointer | **Confirmed-Open** | The splice-specific member-access path checks pointer/record shape but passes the original `Base` directly into the normal member builder (`clang/lib/Sema/SemaExprMember.cpp:1233-1257`, `1330-1335`). It performs no lvalue-to-rvalue conversion before the `IsArrow` build, matching the reported assertion requiring a pointer prvalue. | High |

The batch splits into a few clear clusters. Issues #311, #312, #314, #319, #321, #326, #327, #342, and #350 still correspond directly to visible unhandled cases in current source; #322 and #333 are already fixed in this fork, while #313 is covered by the current linkage-specifier traversal. Issues #331, #332, and #346 need build-enabled confirmation because their exact behavior depends on constant-evaluator/parser state rather than an obviously unconditional source-path failure.