| Issue # | Title (short) | Disposition | Evidence/Reasoning (2-4 sentences, cite file:line where you looked) | Confidence |
|---:|---|---|---|---|
| 230 | Reflecting `std::int32_t` | Out-of-Scope | libc++ imports C compatibility typedefs with `UsingShadowDecl`; the fork intentionally rejects these unless entity-proxy reflection is enabled (`clang/lib/Sema/SemaReflect.cpp:1044-1050`, `1314-1336`). The issue’s requested behavior depends on unresolved WG21 semantics for reflecting using-declarations and implementation-defined `<cstdint>` exposure. | High |
| 232 | `template for` and `display_string_of` | Needs-Build-To-Verify | The current `pretty_printer` still uses captured local variables inside expansion statements (`libcxx/include/meta:2960-2970`), matching the reported `result`/`Idx` capture pattern. Existing tests exercise `template for` and `display_string_of`, but none reproduces this nested `type_of(field)` case (`libcxx/test/std/experimental/reflection/reflection-ex-universal-formatter.sh.cpp:43-58`). Repro: the issue’s `print_fields<T>` template over `define_static_array(nonstatic_data_members_of(...))`, with `display_string_of(type_of(field))` in the body. | Medium |
| 234 | Protected base member reflection | Needs-Build-To-Verify | `MetaActionsImpl::IsAccessible` explicitly permits access when the naming class derives from the target’s declaring class (`clang/lib/Sema/SemaReflect.cpp:214-250`), so source inspection does not establish the reported rejection. Existing visibility tests cover protected members and bases, but not `^^A::protected_virtual_function` from a derived-class context (`libcxx/test/std/experimental/reflection/member-visibility.pass.cpp:65-90`). Repro: the issue’s `A`/`B` example with `static_assert(is_protected(^^A::protected_virtual_function));`. | Medium |
| 235 | Ambiguous constructor reflection | Out-of-Scope | This is an unresolved design question: `^^S::S` denotes multiple constructors, and the fork intentionally rejects unresolved overload sets (`clang/lib/Sema/SemaReflect.cpp:1398-1430`). Choosing a constructor or defining overload-set reflection semantics requires WG21 resolution rather than a fork correctness fix. | High |
| 237 | Alias of closure type loses alias identity | Needs-Build-To-Verify | The fork’s display logic treats closure types without identifiers as `"(anonymous type)"` (`libcxx/include/meta:3090-3120`), while ordinary named aliases are tested separately (`libcxx/test/std/experimental/reflection/names.pass.cpp:35-45`, `120-130`). Source reading cannot determine whether `typename[:cr:]` is required to preserve the alias declaration or intentionally produces the underlying closure type. Repro: the issue’s `cr = ^^decltype(closure)`, `using ct = typename[:cr:]`, followed by `is_type_alias(^^ct)` and `display_string_of(^^ct)`. | Low |
| 239 | Closure `operator()` reported overloaded | Needs-Build-To-Verify | The fork rejects unresolved lookup expressions when overload resolution cannot select one candidate (`clang/lib/Sema/SemaReflect.cpp:1412-1430`), and the reported generic-lambda operator is a function template rather than an ordinary non-template member. Current pretty-printing has separate handling for operator function templates (`libcxx/include/meta:2990-3015`), but that does not prove the reflection lookup path is correct for the dependent alias case. Repro: the issue’s `extract_closure_invoke_operator_refl` template and `return ^^closure_t::operator();`. | Medium |
| 245 | Protected member as reflected template argument | Not-Applicable | This fork deliberately models reflection access through `access_context`; the header exposes `current()`, `unprivileged()`, `unchecked()`, and `via()` (`libcxx/include/meta:1053-1090`). Reflection lookup and subsequent splice access therefore retain ordinary access-context effects, rather than guaranteeing that a reflection formed at an inaccessible call site bypasses access checks. | High |
| 246 | Order-dependent `define_static_array` | Needs-Build-To-Verify | The current implementation routes `define_static_array` through `reflect_constant_array` and extracts a static array (`libcxx/include/meta:2642-2660`); no source-level ordering dependence is visible there. Recent fork work fixed stale consteval-only caching (`01836b3333d5`, implemented in `clang/lib/AST/Decl.cpp:5426-5475`) and reflection/APValue instability, so this may already be resolved, but compilation is required. Repro: two consecutive `constexpr auto arr_0/arr_1 = define_static_array(vector<pair<int, meta::info>>{{42, ^^int}})` declarations; remove `arr_0` and compare. | Medium |
| 252 | VS Code `__has_feature(reflection)` | Out-of-Scope | The issue explicitly identifies VS Code IntelliSense as the failing implementation, not Clang. The fork’s `<meta>` correctly gates facilities on compiler feature macros (`libcxx/include/meta:819-830`); editor feature-macro emulation is outside this repository’s conformance scope. | High |
| 253 | ICE during recursive reflection in modules | Needs-Build-To-Verify | The fork has module/annotation serialization work and expansion-statement tests (`f63157a8d87c`; `libcxx/test/std/experimental/reflection/example-module.cppm:80-92`), but no exact recursive `nonstatic_data_members_of` module reproducer. Source inspection cannot determine whether the upstream ICE survives current serialization and tree-transformation changes. Repro: build the attached module example, recursively instantiate a `template for` over `nonstatic_data_members_of`, and precompile/import the module. | Medium |
| 254 | `define_static_string` hits constexpr step limit | Confirmed-Open | `define_static_string` directly passes the entire input to `reflect_constant_string` and extracts the result (`libcxx/include/meta:2655-2660`); there is no chunking, step-limit management, or alternate long-string path. The reported failure is therefore consistent with current architecture, although exact threshold depends on compiler constexpr limits. | High |
| 256 | `__is_consteval_only` and defaulted special members | Already-Fixed | Current `RecordDecl::isConstevalOnly()` determines consteval-only status from fields and bases, not whether copy/move constructors or assignments are explicitly defaulted (`clang/lib/AST/Decl.cpp:5426-5475`). The fork’s `p3603-consteval-only.pass.cpp` and `meta-type-traits.pass.cpp` cover the consteval-only reflection machinery (`libcxx/test/std/experimental/reflection/p3603-consteval-only.pass.cpp:1-40`, `meta-type-traits.pass.cpp:65-75`), and commit `01836b3333d5` replaced stale eager classification with lazy recomputation. | High |
| 259 | Windows build errors | Out-of-Scope | This is a platform/build-system failure in the upstream snapshot, with errors in third-party benchmark compilation rather than reflection semantics. The fork also uses a deliberately different two-tree build architecture documented in the repository instructions; no current fork source evidence ties this issue to reflection correctness. | High |
| 262 | Annotation crash with modules | Already-Fixed | The fork added custom serialization for `CXX26AnnotationAttr` and module regression tests (`f63157a8d87c`; `clang/include/clang/Basic/Attr.td`, `clang/lib/AST/ExprConstantMeta.cpp:6649-6760`). The tests explicitly precompile/import annotated modules and query annotations (`libcxx/test/std/experimental/reflection/annotation-module-serialization.sh.cpp:1-14`). This directly addresses the reported annotation/module instability. | High |
| 264 | Constant evaluation of arrow splices | Needs-Build-To-Verify | The issue concerns constant-expression evaluation after a nontrivial `meta::info` computation; the relevant splice path first evaluates the splice operand as a constant expression (`clang/lib/Sema/SemaReflect.cpp:1730-1745`), but source inspection cannot establish whether `value->[:field:]` loses the required pointer/object provenance. Repro: compare `&value->[:field:]` with `&static_cast<const Value&>(*value).[:field:]` inside a constant-evaluation context, using a computed `field` reflection. | Medium |
| 265 | Anonymous union members unavailable for splicing | Already-Fixed | The fork contains a dedicated anonymous-union regression test verifying both direct member and pointer-to-member splice access (`libcxx/test/std/experimental/reflection/anon-union.pass.cpp:18-31`). Commits `f33742c88aa3` and `2ea0a79fe7bb` normalize anonymous fields to injected indirect fields and fix direct access through nested anonymous records (`clang/lib/Sema/SemaReflect.cpp:1700-1725`). | High |
| 273 | Dependent function-template reflection seen as overload set | Already-Fixed | Current `ActOnCXXReflectExpr` preserves dependent template reflections instead of degrading them to ordinary unresolved lookup (`clang/lib/Sema/SemaReflect.cpp:998-1030`). Commit `41fa327e7d63` specifically changed dependent/splice-scoped reflection handling and fixed the associated transformation crash; the surrounding code documents that it prevents reflection from becoming indistinguishable from an overload set. | High |

The batch splits into three main groups: several upstream reports are already covered by fork-specific work, especially anonymous-union splicing (#265), annotation/module serialization (#262), dependent template reflection (#273), and consteval-only classification (#256). A second group remains plausible but requires actual compilation because module serialization, constant-expression provenance, and expansion-statement transformation are difficult to prove from source alone (#232, #234, #237, #239, #246, #253, #264). The remaining reports are intentionally outside this fork’s correctness scope: unresolved reflection design questions (#230, #235), editor integration (#252), and general platform build failures (#259).