| Issue # | Title (short) | Disposition | Evidence/Reasoning (2-4 sentences) | Confidence |
|---:|---|---|---|---|
| 275 | clangd crashes while code compiles | Needs-Build-To-Verify | Report is a clangd/ASTReader crash while loading external declarations, not a direct reflection semantic failure. This fork has changed substantially since the reported `9ffb96e` clangd build and has annotation/PCH/module coverage, but no local reproduction is included; reproduce with the referenced project and `clangd -freflection-latest` before classifying further. | Low |
| 276 | Splice type aliases treated as identical | Confirmed-Open | Dependent splice types are represented by `DependentReflectionSpliceType` and profiled from the splice pointer (`clang/lib/AST/Type.cpp:4247-4258`), while nondependent splice types are canonicalized to the underlying type in `ASTContext::getReflectionSpliceType` (`clang/lib/AST/ASTContext.cpp:6171-6185`). No fork regression test covers the two dependent aliases in the issue, so the reported alias-collapse remains plausible without a build. | Medium |
| 280 | `has_parent` missing | Confirmed-Open | The fork’s `<meta>` declares and implements `parent_of`, but has no `has_parent` declaration or implementation (`libcxx/include/meta:56-60`, `:934-942`). This is a missing C++26 library feature rather than a crash or wrong result. | High |
| 281 | ICE from `annotations_of(^^member)` | Already-Fixed | The reported code reflects the loop variable declaration, not its `std::meta::info` value. Current Sema handles a reflected declaration through the ordinary `DeclRefExpr` path (`clang/lib/Sema/SemaReflect.cpp:1371-1377`), and valid annotation reflection is extensively covered by `p3394-annotations.pass.cpp:89-110` and `:131-142`; the old `ActOnCXXReflectExpr` crash path is no longer evident. | Medium |
| 286 | Same-named function-template reflections collide | Confirmed-Open | The fork now routes reflection-valued NTTPs through `mangleReflection` (`clang/lib/AST/ItaniumMangle.cpp:6728`), but its `ReflectionKind::Template` case still mangles only the template name (`:4932-4937`). There is no discriminator for same-named overloaded function templates, and the related `mangleReflection` history contains only the general reflection-NTTP restoration (`698fc39db256`), not the overload fix. | High |
| 288 | Reentrant constant evaluation causes UAF | Confirmed-Open | `HandleImmediateInvocations` iterates and evaluates candidates while holding `ExpressionEvaluationContextRecord &Rec` (`clang/lib/Sema/SemaExpr.cpp:18396-18402`, `:18473-18481`); `PopExpressionEvaluationContext` retains that reference across the call and subsequent accesses (`:18551-18553`, `:18592-18611`). No fork commit or regression test addresses reallocation of `ExprEvalContexts`; the supplied 64-level `chain` reproducer should be build-tested. | High |
| 290 | Entity-proxy queries and mangling crash | Confirmed-Open | Entity proxies are deliberately emitted by `members_of` when enabled (`clang/lib/AST/ExprConstantMeta.cpp:1475-1481`), but several query handlers still terminate on `ReflectionKind::EntityProxy`, including `is_constructor` (`:5346-5375`), `is_destructor` (`:5515-5526`), and `is_special_member_function` (`:5562-5570`). The Itanium mangler also still mangles a proxy as a `UsingShadowDecl` name without target-aware discrimination (`clang/lib/AST/ItaniumMangle.cpp:4947-4951`), while `entity-proxies.pass.cpp` covers ordinary proxy identity but not these failing queries. | High |
| 292 | `AttributedType` blinds function queries | Already-Fixed | Function-type queries explicitly use `getAs<FunctionProtoType>()` to see through attribute sugar (`clang/lib/AST/ExprConstantMeta.cpp:1622-1629`), and the same file’s history records the fix as `1dee6d809821`. Regression coverage is present in `attributed-function-type-queries.pass.cpp`. | High |
| 294 | `can_substitute`/`substitute` crash on invalid formed types | Confirmed-Open | Although template-argument-list diagnostics can be suppressed (`clang/lib/Sema/SemaReflect.cpp:350-373`), declaration substitution still calls Sema directly and assumes success (`:406-423`). The metafunction then asserts non-null results for function, variable, and concept substitutions (`clang/lib/AST/ExprConstantMeta.cpp:3590-3619`), matching the issue’s invalid-reference-to-void failure modes. | High |
| 296 | `members_of` eagerly instantiates member bodies | Already-Fixed | `EnsureInstantiated` was changed to implicit-instantiation semantics by commit `7220baffd57e`, with an explicit regression test. `members-of-lazily-ill-formed-bodies.pass.cpp:18-28` documents the former eager-body bug and tests order-independent enumeration at `:75-89`. | High |
| 298 | Deduction-guide reflection mangler ICE | Confirmed-Open | `ReflectionKind::Template` still calls `mangleTemplateName` unconditionally (`clang/lib/AST/ItaniumMangle.cpp:4932-4937`), while ordinary deduction-guide names are explicitly an unsupported mangling case in the same mangler. No deduction-guide reflection regression test appears under `clang/test/Reflection` or libc++ reflection tests. | High |
| 300 | Same-headed member-template reflections collide | Confirmed-Open | The current template-reflection mangling has no ODR/type/ref-qualifier discriminator (`clang/lib/AST/ItaniumMangle.cpp:4932-4937`). `ODRHash::AddFunctionDecl` intentionally returns for declarations inside class-template specializations (`clang/lib/AST/ODRHash.cpp:670-690`), leaving exactly the specialization-context gap described by the issue. | High |
| 302 | Reopened namespace reflections compare unequal | Already-Fixed | `profileReflection` now canonicalizes namespace declarations (`clang/lib/AST/APValue.cpp:575-584`), preserving namespace-alias distinction. `namespace-reflection-equality-reopened.pass.cpp:19-31` and `:70-95` directly regression-test reopened namespaces, nested reopening, and aliases; fix commit: `2245a73e94f5`. | High |
| 303 | Reopened namespace walk truncates members | Confirmed-Open | `isReflectableDecl` only checks redeclarations by lexical context (`clang/lib/AST/ExprConstantMeta.cpp:1457-1467`), and `findIterableMember` starts cross-block traversal from `*NSDecl->decls_begin()` (`:1535-1545`) while otherwise stepping through the semantic context (`:1519-1549`). Those conditions still admit an out-of-line class-member definition and then leave the namespace chain, with no targeted regression test present. | High |
| 304 | `is_complete_type` fails through aliases | Already-Fixed | The implementation now desugars aliases before finding and instantiating the underlying type (`clang/lib/AST/ExprConstantMeta.cpp:4865-4880`). Commit `7f0f89cc7e75` added the fix and `is-complete-type-alias-sugar.pass.cpp` covers alias, ordering, and incomplete-type cases. | High |
| 308 | Tracking issue for #286–#304 | Out-of-Scope | This record is an aggregate tracking/reporting issue, not an independent compiler or library behavior claim. Its individual entries are triaged separately above; there is no additional disposition to apply to the tracker itself. | High |
| 309 | Dependent splice ICE during `auto` NTTP deduction | Confirmed-Open | The dependent splice construction still creates a `CXXSpliceExpr` with a null model (`clang/lib/Sema/SemaReflect.cpp:1596-1601`, `:1904-1905`), while expression classification dereferences that model unconditionally (`clang/lib/AST/ExprClassification.cpp:281-293`). The issue’s `requires { typename probe<([:mem:])>; }` path therefore remains a direct logical null-dereference candidate. | High |

The batch splits into clear families. Issues #292, #296, #302, and #304 are already fixed in this fork with dedicated regression tests and commits. Issues #286, #298, and #300 remain one Itanium reflection-mangling family: general reflection NTTP mangling exists, but template overload, deduction-guide, and specialization-context discrimination are still incomplete. #290 and #303 remain enumeration/identity failures around entity proxies and namespace redeclarations, while #288 and #309 expose separate reentrancy/null-model robustness gaps.