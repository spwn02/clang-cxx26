| Issue # | Title (short) | Disposition | Evidence/Reasoning (2-4 sentences, cite file:line where you looked) | Confidence |
|---:|---|---|---|---|
| 185 | Annotation API changed in R1 | **Confirmed-Open** | `<meta>` still declares the obsolete `annotations_of(info, info)`, `annotation_of_type`, and `annotate` APIs; `annotations_of_with_type` is absent (`libcxx/include/meta:358-364`, `2085-2128`). Existing tests also exercise the old overload and removed API (`p3394-annotations.pass.cpp:97-100`). | High |
| 187 | Compilation never ends | **Needs-Build-To-Verify** | The snapshot contains no reproducer, only a Godbolt link, so source inspection cannot identify the infinite-compilation path. A build-enabled follow-up should run the linked example with the line-68 construct enabled; the issue has no comments and is therefore weakly corroborated. | Low |
| 188 | `display_string_of(dealias(...))` not constant expression | **Needs-Build-To-Verify** | The fork’s `dealias`/pretty-printer path is substantial: `display_string_of` dispatches through `pretty_printer` (`libcxx/include/meta:3303-3308`), while rendering recursively queries `type_of` and related metafunctions (`libcxx/include/meta:2960-2990`). No regression test covers `display_string_of(dealias(...))`; run the issue’s `max_element` example, especially `display_string_of(dealias(^^decltype(std::ranges::max_element(s))))`. | Medium |
| 189 | Upstream to LLVM | **Out-of-Scope** | This is an adoption/distribution request, not a correctness or conformance defect in this fork. The fork has already diverged architecturally and includes independent C++26 and Contracts work; whether upstream LLVM accepts reflection is external project policy. | High |
| 200 | `parent_of` wrong for class-template aliases | **Confirmed-Open** | `parent_of` on a type reflection first calls `findTemplateOfType`, then `parentOf(findTypeDecl(...))` (`clang/lib/AST/ExprConstantMeta.cpp:3055-3060`). The implementation does not preserve the alias declaration layer, matching the reported `using A = S<int>` failure; current tests cover ordinary aliases and specializations but not this exact alias-of-specialization case (`related-reflections.pass.cpp:104-121`). | High |
| 203 | Unbalanced diagnostic parentheses | **Needs-Build-To-Verify** | The reported problem is diagnostic formatting produced while synthesizing expansion-statement range machinery, not a clearly visible reflection semantic branch. Current expansion analysis builds synthetic `begin`/`end` calls through overload resolution (`clang/lib/Sema/SemaExpand.cpp:82-121`), but source reading cannot establish the final printed type spelling. Reproduce with `void f(){ template for(auto x : f); }`. | Medium |
| 204 | ICE: `template for` over overload set | **Needs-Build-To-Verify** | Current expansion analysis still routes range initialization through overload resolution (`clang/lib/Sema/SemaExpand.cpp:82-121`), and there is no dedicated regression test for an overloaded function range. The issue’s shown diagnostic-plus-ICE could have changed after the LLVM 22 reconciliation, so run `void f(); void f(int){} void g(){ template for(auto x : f); }`. | Medium |
| 205 | ICE: templated lambda plus `define_static_array` | **Already-Fixed** | Fork history contains the targeted fix `f72d85e5a0fd` (“Fix ICE when using expansion statements inside lambda (#214)”), followed by current expansion-statement handling that synthesizes the range declaration outside nested `ExpansionStmtDecl` contexts and marks it implicit (`clang/lib/Sema/SemaExpand.cpp:148-173`). This is the same lambda/expansion ICE shape reported here. | High |
| 208 | CRTP constexpr degradation | **Needs-Build-To-Verify** | The snapshot gives only a Godbolt link and a schematic description; no exact constexpr expression or implementation detail is available to prove whether this is a reflection-induced issue or a generic template-instantiation bug. Minimal follow-up: instantiate `template<class T> struct B { static constexpr std::size_t size = /*T-dependent constexpr*/; static constexpr std::array<int,size> arr{}; }; struct A : B<A> {};` and test `B<A>::arr`. | Low |
| 210 | Capturing lambda inside expansion statement | **Already-Fixed** | Fork history contains the directly named fix `f0a3e5e612db` (“BUG: Lambda capture within expansion statements (#250)”) and a follow-up `0f70ed5eb99e` for another lambda/expansion capture case. Current expansion synthesis explicitly walks out of nested expansion declaration contexts before creating synthetic variables (`clang/lib/Sema/SemaExpand.cpp:148-173`); the original issue has no newer discussion. | High |
| 211 | Static enum member has wrong `type_of` | **Needs-Build-To-Verify** | `type_of` for declarations obtains the declared `ValueDecl` type and desugars aliases (`clang/lib/AST/ExprConstantMeta.cpp:2982-2994`), which looks correct for a static enum member but does not rule out an enum-specific AST/type representation problem. Existing tests cover enum values and enum classification (`to-and-from-values.pass.cpp:131-162`, `entity-classification.pass.cpp:450-524`) but not a static enum data member; run the linked reproducer. | Medium |
| 212 | ICE in `if constexpr` optional extraction | **Needs-Build-To-Verify** | The snapshot provides no body beyond `if constexpr (auto opt = func<...>(); opt_value)` and a link, so the failing template/optional instantiation path cannot be located reliably from source. Minimal follow-up should preserve that form with the exact `func` and optional type from the link, then test both instantiated and non-instantiated branches. | Low |
| 215 | Empty `reflect_constant_array` result | **Already-Fixed** | The library now deliberately maps an empty range to `__define_static::EmptyArray`, which is `std::array<ValTy, 0>` (`libcxx/include/meta:2598-2616`). The shipped test asserts object/class classification and equality with `^^const std::array<int, 0>` (`static-arrays.pass.cpp:64-70`); history contains the targeted fix `5dafd8cc4a45` (“fix reflection in case of empty array (#223)”). | High |
| 220 | `display_string_of(type_of(undeduced))` hangs | **Needs-Build-To-Verify** | `return_type_of` accepts a function type and immediately desugars its return type without an explicit undeduced-return guard (`clang/lib/AST/ExprConstantMeta.cpp:6565-6594`), making the reported recursion/hang plausible. However, no source-only proof establishes whether later printer constraints terminate; run `display_string_of(type_of(^^s_undeducible::operator()))` with `auto operator()();`. | Medium |
| 221 | `expected<bool,int>` in `vector` fails | **Needs-Build-To-Verify** | The fork contains the same C++26 constrained equality expression implicated by the report (`libcxx/include/__expected/expected.h:1164-1173`), although the three upstream expected commits named in the issue are present in history. Since the failure depends on compiler constraint normalization and CTAD, run the exact `std::vector vector { value(), value() };` snippet before calling it open. | Medium |
| 222 | `define_aggregate` nested incomplete type in template argument | **Needs-Build-To-Verify** | `define_aggregate` requires an incomplete record and delegates completion to `Meta.DefineAggregate` (`clang/lib/AST/ExprConstantMeta.cpp:6092-6119`). The Sema implementation has explicit support for `ClassTemplateSpecializationDecl` arguments (`clang/lib/Sema/SemaReflect.cpp:638-710`), but the reported nested non-template `inside_S` case follows a different path and has no matching test; run the issue’s `S<int,float,double>` lambda/NTTP example. | Medium |
| 225 | `meta::exception` unimplemented | **Confirmed-Open** | `<meta>` has no `meta::exception` declaration or implementation; the nearby API synopsis jumps from annotations to attributes (`libcxx/include/meta:358-373`), and repository-wide search finds no reflection exception type. The fork has implemented constexpr throw/catch machinery in `ExprConstant.cpp` (`clang/lib/AST/ExprConstant.cpp:881-893`, `6635-6689`), so this is now a missing library reflection facility rather than an absence of the P3068 compiler prerequisite. | High |

The batch shows three clear historical fixes for expansion/array problems: #205 and #210 are covered by dedicated lambda/expansion fixes, while #215 is covered by the empty-array implementation and regression test. The remaining reflection-specific correctness gaps cluster around alias preservation (#200), annotation API revision drift (#185), and missing `meta::exception` (#225). Several reports cannot be responsibly resolved without builds because their snapshot bodies omit the actual reproducer and provide only Godbolt links (#187, #208, #212), while #189 is purely a distribution/upstreaming request.